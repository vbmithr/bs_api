true : bin_annot, package(bytes), warn(-40)

<src> : include
<test> : include

<*/*>: package(core), \
       package(async), \
       package(bs-devkit), \
       package(nocrypto), \
       package(hex), \
       package(dtc), \
       package(cohttp.async), \
       package(websocket.async), \
       package(wamp.msgpck), \
       package(jsonm), \
       thread