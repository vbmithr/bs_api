true : bin_annot, safe_string, package(bytes), warn(-40)

<src> : include
<test> : include

<*/*>: package(core), \
       package(async), \
       package(bs_devkit), \
       package(nocrypto), \
       package(hex), \
       package(dtc), \
       package(cohttp.async), \
       package(websocket.async), \
       package(wamp.yojson), \
       thread